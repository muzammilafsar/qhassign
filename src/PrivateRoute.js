import React, { useEffect } from 'react';
import { Route, Redirect } from "react-router-dom";

function PrivateRoute({ component, isAuthenticated , ...all }) {
    return (
        <React.Fragment>
     
        </React.Fragment>
    );
}

export default PrivateRoute;
